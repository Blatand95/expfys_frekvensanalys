fs = 50e3; % 50kS/s
T_s = 1/fs; % sample period
n_rl = 2^12; % record-length or number of samples
t_T = n_rl*T_s; % allocation time
t = 0:T_s:t_T-T_s; % sample time
f_max = fs/2;

S = 2*sin(10e3*t) + sin(15e3*t + pi/6); % time-domain signal
F = fft(S,n_rl);
P2 = abs(F);
P2 =